import type { ColorScheme } from "@/types/project";

export const PILL_COLORS: Record<ColorScheme, { background: string; color: string }> = {
  blue: { background: "#E6F1FB", color: "#0C447C" },
  purple: { background: "#EEEDFE", color: "#3C3489" },
  green: { background: "#EAF3DE", color: "#27500A" },
  amber: { background: "#FAEEDA", color: "#854F0B" },
  coral: { background: "#FDEAE3", color: "#993C1D" },
};
